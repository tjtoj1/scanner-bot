name: Monitor LAB
on:
  schedule:
    - cron: '* 13-21 * * 1-5'
  workflow_dispatch:
concurrency:
  group: monitor-lab
  cancel-in-progress: false
permissions:
  contents: write
jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run LAB monitor
        env:
          ALPACA_KEY_3:    ${{ secrets.ALPACA_KEY_3 }}
          ALPACA_SECRET_3: ${{ secrets.ALPACA_SECRET_3 }}
          TG_TOKEN:        ${{ secrets.TG_TOKEN }}
          MODE:            monitor
        run: node scan_lab.js
      - name: Commit state
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git diff --quiet && git diff --staged --quiet && exit 0
          git commit -m "lab monitor [skip ci]"
          for i in 1 2 3 4 5; do
            git push && exit 0
            git fetch origin main
            git rebase --abort 2>/dev/null || true
            git checkout -f main 2>/dev/null || true
            git pull origin main -X ours --no-edit 2>/dev/null || {
              git checkout --ours state_lab.json 2>/dev/null || true
              git checkout --ours strategy_lab.json 2>/dev/null || true
              git checkout --ours outcomes_lab.jsonl 2>/dev/null || true
              git add -A
              GIT_EDITOR=true git merge --continue 2>/dev/null || true
            }
            git add -A
            git diff --staged --quiet || git commit -m "lab monitor merge [skip ci]" --allow-empty
            sleep $((i*2))
          done
          exit 0
